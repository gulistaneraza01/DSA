package Basic;

public class StringDemo {
    public static void main(String[] args) {

        // int num = 123;

        String numStr = "123";

        // String firstName = "gulistane";
        // firstName = "raza";

        // String lastName = "raza hello";
        // String fullName = firstName.concat(lastName);
        // // split() returns a String array, not a printable string directly
        // String[] parts = lastName.split(" ");

        // System.out.println(firstName.substring(2, 6));

        // System.out.println(firstName.trim());

        // System.out.println(fullName.charAt(0));
        // System.out.println(lastName.charAt(3));

        // compareTo returns an int, not a boolean. Check if result is 0 for equality.

        // if (firstName.compareTo(lastName) == 0) {
        // System.out.println(true);
        // } else {
        // System.out.println(false);
        // }

        // for (int i = 0; i < fullName.length(); i++) {
        // System.out.println(fullName.charAt(i));
        // }
    }
}
